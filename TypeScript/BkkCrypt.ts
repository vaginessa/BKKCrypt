/**
 * BkkCrypt - Encrypts and returns the input string using the famous BKK cryptographic method.
 * @param input - String to be safely encrypted by the algorythm.
 * @return Safely encrypted string output of the program.
 */
const BkkCrypt = (input: string) => input;

export { BkkCrypt }
